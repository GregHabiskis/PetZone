import { ProductCard } from '@/components/product-card';import { products } from '@/lib/data';export default function OfferPage(){return <><section className="page-hero"><div className="shell"><p className="eyebrow">OFFER ZONE</p><h1>Useful savings, clearly explained.</h1><p>Current Pet Zone offers with prices and availability shown upfront.</p></div></section><section className="section shell"><div className="product-grid">{products.filter(p=>p.compareAtPrice).map(p=><ProductCard key={p.id} product={p}/>)}</div></section></>}
