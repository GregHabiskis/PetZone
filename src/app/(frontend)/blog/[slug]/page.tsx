import type { Metadata } from 'next';import { notFound } from 'next/navigation';import { blogPosts } from '@/lib/blog-data';export function generateStaticParams(){return blogPosts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const post=blogPosts.find(p=>p.slug===slug);return post?{title:post.title,description:post.excerpt}:{}}export default async function BlogPostPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=blogPosts.find(p=>p.slug===slug);if(!post)notFound();return <><section className="page-hero"><div className="shell"><p className="eyebrow">{post.category} · {post.publishedAt.slice(0,10)}</p><h1>{post.title}</h1></div></section><section className="section shell"><article className="content-copy"><p className="hero-lead">{post.excerpt}</p><aside className="form-card"><h2>Health information note</h2><p>This guide supports everyday care and is not a diagnosis. If your pet is unwell, contact a qualified veterinarian. For urgent symptoms, seek emergency veterinary care immediately.</p><a className="primary-button" href="/vet-care">Request a vet appointment</a></aside></article></section></>}
