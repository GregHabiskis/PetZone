import Image from 'next/image';import { notFound } from 'next/navigation';import { products } from '@/lib/data';import { formatBDT } from '@/lib/commerce';import { ProductCard } from '@/components/product-card';import { ProductPurchaseAction } from '@/components/product-purchase-action';export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)notFound();return <><section className="section shell"><div className="vet-feature"><div className="vet-image"><Image src={product.image} alt={product.name.en} fill sizes="(max-width:767px) 100vw,50vw" style={{objectFit:'contain',padding:'32px'}}/></div><div className="vet-copy"><p className="eyebrow">{product.brand}</p><h1>{product.name.en}</h1><p>{product.name.bn}</p><h2>{formatBDT(product.price)}</h2><p className={product.inStock?'stock':'out'}>{product.inStock?'In stock · usually ships in 1–2 days':'Out of stock'}</p><p>Authentic product sourced for Pet Zone customers in Bangladesh. Select the right pack size and follow product feeding guidance.</p><ProductPurchaseAction product={product}/><p><a href="https://wa.me/88001787101001">Need help choosing? Chat with Pet Zone.</a></p></div></div></section><section className="section shell content-copy"><h2>Overview</h2><p>Product details, ingredients, feeding guidance, storage information, origin, returns eligibility and health notes are managed in localized Payload CMS fields.</p><h2>Shipping & returns</h2><p>Delivery time and eligibility are confirmed at checkout based on stock and destination.</p></section><section className="section shell"><h2>More for your pet</h2><div className="product-grid">{products.filter(p=>p.id!==product.id).slice(0,4).map(p=><ProductCard key={p.id} product={p}/>)}</div></section></>}
